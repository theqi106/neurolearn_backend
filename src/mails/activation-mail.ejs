<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Academix Account Activation</title>
    <style>
      @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap');
      
      body {
        margin: 0;
        padding: 0;
        font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
        background-color: #f8fafc;
        color: #1f2937;
        line-height: 1.5;
      }

      .container {
        max-width: 600px;
        margin: 40px auto;
        background-color: #ffffff;
        border-radius: 12px;
        overflow: hidden;
        box-shadow: 0 4px 24px rgba(0, 0, 0, 0.08);
      }

      .header {
        background: linear-gradient(135deg, #2563eb 0%, #1d4ed8 100%);
        color: white;
        text-align: center;
        padding: 40px 24px;
      }

      .header h1 {
        margin: 0;
        font-size: 28px;
        font-weight: 700;
        letter-spacing: -0.5px;
      }

      .logo {
        font-size: 32px;
        margin-bottom: 12px;
        display: inline-block;
      }

      .body {
        padding: 36px 32px;
        font-size: 16px;
      }

      .body h2 {
        color: #1e40af;
        font-size: 20px;
        font-weight: 600;
        margin-top: 0;
        margin-bottom: 24px;
      }

      .activation-code {
        background-color: #eff6ff;
        color: #1d4ed8;
        padding: 16px 24px;
        border-radius: 8px;
        font-size: 28px;
        font-weight: 700;
        letter-spacing: 2px;
        text-align: center;
        margin: 32px 0;
        cursor: pointer;
        transition: all 0.2s ease;
        border: 1px dashed #bfdbfe;
        user-select: all;
      }

      .activation-code:hover {
        background-color: #dbeafe;
        transform: translateY(-1px);
      }

      .activation-code.copied {
        background-color: #dcfce7;
        color: #166534;
        border-color: #bbf7d0;
      }

      .divider {
        height: 1px;
        background-color: #e5e7eb;
        margin: 28px 0;
      }

      .footer {
        padding: 24px 32px;
        text-align: center;
        font-size: 14px;
        color: #6b7280;
        background-color: #f9fafb;
        border-top: 1px solid #e5e7eb;
      }

      .footer a {
        color: #6b7280;
      }

      a {
        color: #2563eb;
        text-decoration: none;
        font-weight: 500;
      }

      a:hover {
        text-decoration: underline;
      }

      .text-muted {
        color: #6b7280;
        font-size: 14px;
      }

      .text-center {
        text-align: center;
      }

      .mt-3 {
        margin-top: 12px;
      }

      .mb-4 {
        margin-bottom: 24px;
      }

      /* Responsive adjustments */
      @media only screen and (max-width: 640px) {
        .container {
          margin: 20px auto;
          border-radius: 0;
        }
        
        .header {
          padding: 32px 20px;
        }
        
        .header h1 {
          font-size: 24px;
        }
        
        .body {
          padding: 28px 20px;
        }
        
        .activation-code {
          font-size: 24px;
          padding: 14px 20px;
        }
      }

      @media only screen and (max-width: 480px) {
        .activation-code {
          font-size: 20px;
          padding: 12px 16px;
        }
        
        .body {
          font-size: 15px;
        }
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="header">
        <div class="logo">🎓</div>
        <h1>Welcome to Academix</h1>
      </div>
      <div class="body">
        <h2>Account Activation</h2>
        
        <p>Hello <strong><%= user.name %></strong>,</p>
        <p>Thank you for joining Academix! To activate your account, please use the verification code below:</p>

        <!-- Activation Code -->
        <div 
          class="activation-code" 
          id="activationCode"
          onclick="copyCodeToClipboard(this)"
          title="Click to copy"
        >
          <%= activationCode %>
        </div>
        
        <p class="text-muted text-center">The code will expire in 5 minutes. Please do not share it with anyone.</p>
        
        <div class="divider"></div>
        
        <p class="text-muted">If you didn't request this, you can safely ignore this email. Your account won't be activated until you verify your email address.</p>
        <p class="text-muted mt-3">Need help? <a href="mailto:support@academix.vn">Contact our support team</a> and we'll be happy to assist you.</p>
      </div>
      <div class="footer">
        <p>&copy; 2025 Academix. All rights reserved.</p>
        <p class="text-muted">123 Education Street, Hanoi, Vietnam</p>
      </div>
    </div>

    <!-- Click-to-copy script -->
    <script>
      function copyCodeToClipboard(el) {
        const code = el.innerText;
        const originalText = el.innerText;
        const originalClass = el.className;
        
        navigator.clipboard.writeText(code).then(() => {
          el.innerText = '✓ Copied to clipboard!';
          el.className = originalClass + ' copied';
          
          setTimeout(() => {
            el.innerText = originalText;
            el.className = originalClass;
          }, 2000);
        }).catch(err => {
          console.error('Failed to copy: ', err);
        });
      }
    </script>
  </body>
</html>